import org.junit.Test;

import static org.junit.jupiter.api.Assertions.*;

public class ElevatorTest {
    /*
    @Test
    public void testGoToFloor() {
        // Create an ElevatorQueue for testing
        ElevatorQueue elevatorQueue = new ElevatorQueue();

        // Create an Elevator instance for testing
        Elevator elevator = new Elevator(1, elevatorQueue);

        // Call the goToFloor method and check if it works as expected
        elevator.goToFloor(3);
        assertEquals(3, elevator.getCurrentFloor());

        elevator.goToFloor(2);
        assertEquals(2, elevator.getCurrentFloor());
    }
    @Test
    public void testOpenandCloseDoor(){
        // Create an ElevatorQueue for testing
        ElevatorQueue elevatorQueue = new ElevatorQueue();

        // Create an Elevator instance for testing
        Elevator elevator = new Elevator(1, elevatorQueue);

        // Check if Elevator door Opens
        elevator.openDoor();
        assertTrue(elevator.isDoorOpen());

        //Check if elevator door Closes
        elevator.closeDoor();
        assertFalse(elevator.isDoorOpen());

    }
     */
}